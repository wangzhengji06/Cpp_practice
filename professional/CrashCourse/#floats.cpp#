#include <cmath>
#include <limits>
#include <print>

using namespace std;

int main() {
  float f1 = std::numeric_limits<float>::infinity();
  println("{}", f1 == 1.0f / 0.0f);
  println("{}", std::isinf(1.0f / 0.0f));
  println("{}", std::isnan(0.0f / 0.0f));
}
